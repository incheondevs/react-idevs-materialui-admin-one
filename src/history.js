import { createBrowserHistory } from 'history';
export default createBrowserHistory();
/** 해시라우터를 사용하려면 위를 주석처리하고 아래 주석해제 **/
// import { createHashHistory } from 'history';
// export default createHashHistory();